# src/Dockerfile.frontend
FROM python:3.9-slim

WORKDIR /app

RUN pip install --no-cache-dir streamlit==1.32.0 requests==2.31.0 pandas==2.1.0

COPY . /app/

EXPOSE 8501

CMD ["streamlit", "run", "dashboard.py", "--server.port=8501", "--server.address=0.0.0.0"]